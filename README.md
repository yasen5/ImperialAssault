# ImperialAssault

## Gradle

This project now includes a Gradle build.

Common commands:

```bash
gradle run
gradle runServer
gradle runClient --args="127.0.0.1 5050 REBEL_1"
gradle runPathTest
gradle build
```

The default application entry point is `Runner`.
